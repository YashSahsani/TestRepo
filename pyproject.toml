[project]
name = "performance-review"
version = "0.1.0"
description = "Hackathon prototype for evidence-backed performance review summaries."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
perf-review = "performance_review.cli:main"
